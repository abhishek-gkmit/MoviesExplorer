import { Text } from 'react-native';

import styles from '@screens/moviesList/styles';

function MoviesList() {
  return <Text style={styles.heading}>Movies List</Text>;
}

export default MoviesList;
